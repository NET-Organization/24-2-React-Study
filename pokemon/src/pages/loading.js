import { LoadingPage } from "../components/component";

function Loading() {
    return (
        <LoadingPage>
            <p className="loadingMessage">좀 기다리슈</p>
        </LoadingPage>
    );
}

export default Loading;
